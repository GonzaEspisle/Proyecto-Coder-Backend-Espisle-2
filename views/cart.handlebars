<h1>Detalles del Carrito (ID: {{cart._id}})</h1>


{{#if products}}
    <h2>Productos en Carrito</h2>
    <table>
        <thead>
            <tr>
                <th>Título</th>
                <th>Cantidad</th>
                <th>Precio Unitario</th>
                <th>Subtotal</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            {{#each products}}
                <tr>
                    <td>
                       
                        <strong>{{this.product.title}}</strong>
                        <br><small>ID: {{this.product._id}} | Código: {{this.product.code}}</small>
                    </td>
                    <td>{{this.quantity}}</td>
                    <td>${{this.product.price}}</td>
                    <td>
                        
                        ${{multiply this.product.price this.quantity}}
                    </td>
                    <td>
                        
                        <button onclick="removeProduct('{{../cart._id}}', '{{this.product._id}}')" style="color: red;">❌ Eliminar</button>
                    </td>
                </tr>
            {{/each}}
        </tbody>
    </table>

    <hr>
    
    
    <button onclick="clearCart('{{cart._id}}')" style="margin-top: 20px;">🗑️ Vaciar Carrito Completo</button>

{{else}}
    <div style="padding: 20px; border: 1px dashed #ccc;">
        <p>El carrito está vacío. ¡Añade algunos productos!</p>
    </div>
{{/if}}

